import { _decorator } from 'cc';
import ListItem from '../../util/list/ListItem';
const { ccclass, property } = _decorator;

@ccclass('BuildingAtlasItem')
export class BuildingAtlasItem extends ListItem {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

